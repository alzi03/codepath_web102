// Essentially the same as Character.jsx, but so database doesn't have to store extra information

import { useState, useEffect } from 'react'
import './Display.css'

import { Link } from 'react-router-dom'

export default function FavoriteCard({name}){
  
  
}